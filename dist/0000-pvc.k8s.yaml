apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-shared-pvc-c8fd4c8b
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: rawfile-btrfs
  volumeMode: Filesystem
